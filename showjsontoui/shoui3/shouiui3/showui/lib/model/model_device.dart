class ModelDevice {
  String? id;
  String? name;
  String? working;
  ModelDevice({this.id, this.name, this.working});
}

List<ModelDevice> modelDevice = [
  ModelDevice(id: '1', name: 'Dell', working: 'Working'),
];
